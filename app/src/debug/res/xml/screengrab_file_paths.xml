<?xml version="1.0" encoding="utf-8"?>
<!-- Exposes the app's internal cache dir so the test can serve the demo
     Timeline JSON it copies there as a content:// URI. -->
<paths>
    <cache-path name="cache" path="." />
    <files-path name="files" path="." />
</paths>
